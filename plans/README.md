# Implementation Plans

Generated by the improve skill on 2026-06-12. Execute in the order below unless dependencies say otherwise. Each executor must read the selected plan fully before starting, honor its STOP conditions, and update the status row when done.

These plans were written against commit `bc673f0`. At planning time the worktree already had local changes in `DeskPurge_ProtectedFolders.txt` plus untracked `.vscode/settings.json` and `DeskPurge_Log.txt`; preserve user changes unless a plan explicitly scopes a file.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add Pester safety tests around deletion-target selection | P1 | M | - | DONE |
| 002 | Make Steam library boundaries delete the game folder, not `steamapps` | P1 | S | 001 | DONE |
| 003 | Fail closed when protected-folder config is missing or invalid | P1 | M | 001 | DONE |
| 004 | Correct the context-menu installer default script path | P1 | S | none | DONE |
| 005 | Ignore generated DeskPurge logs | P2 | S | none | DONE |
| 006 | Stream folder-size calculation instead of materializing every item | P3 | S | 001 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- 001 should land before 002, 003, and 006 because those plans touch destructive-path or filesystem traversal behavior; tests should lock down current and intended behavior first.
- 004 and 005 are independent and can be done at any time.

## Verification Baseline

Current CI runs only:

```powershell
Invoke-ScriptAnalyzer -Path . -Recurse -Severity Error,Warning
```

Plan 001 adds Pester tests and wires them into CI. Before plan 001 lands, there is no tracked behavior-test command in the repo.

## Findings Considered And Rejected

- None. The user requested plans for all six audited findings.
